import { createTheme } from "@mui/material/styles";

export default createTheme({
  palette: {
    black: {
      main: "#000000",
      contrastText: "#fff",
    },
  },
  typography: {
    fontFamily: "'Ubuntu', sans-serif",
  },
});
